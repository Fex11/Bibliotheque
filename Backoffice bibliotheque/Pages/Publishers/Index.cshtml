@page
@model Backoffice_bibliotheque.Pages.Publishers.IndexModel

@{
    ViewData["Title"] = "Publishers";
}


<div class="d-flex justify-content-between align-items-center mb-3">
    <a asp-page="Create" class="btn btn-primary">
        <i class="bi bi-plus-lg"></i> Add New Publisher
    </a>
    <input type="text" class="form-control w-25" placeholder="Search..." />
</div>

<table class="table table-striped table-hover table-bordered align-middle">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Publisher[0].Name)</th>
            <th>@Html.DisplayNameFor(model => model.Publisher[0].Country)</th>
            <th>@Html.DisplayNameFor(model => model.Publisher[0].City)</th>
            <th>@Html.DisplayNameFor(model => model.Publisher[0].CreatedAt)</th>
            <th>@Html.DisplayNameFor(model => model.Publisher[0].UpdatedAt)</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Publisher)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Country)</td>
                <td>@Html.DisplayFor(modelItem => item.City)</td>
                <td>@item.CreatedAt.ToString("yyyy-MM-dd")</td>
                <td>@item.UpdatedAt.ToString("yyyy-MM-dd")</td>
                <td class="text-center">
                    <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning me-1">
                        <i class="bi bi-pencil-square"></i> Edit
                    </a>
                    <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-sm btn-info me-1">
                        <i class="bi bi-eye"></i> Details
                    </a>
                    <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                        <i class="bi bi-trash"></i> Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
